class GreetingMessage {
  constructor({ $target, userName }) {
    this.$greetingMessage = document.createElement("div");
  }
  setName = (submittedName) => {};
  render = () => {};
}

export default GreetingMessage;
